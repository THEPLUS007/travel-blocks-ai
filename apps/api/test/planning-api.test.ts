import {describe,expect,it} from 'vitest';import {TestAiProvider,TestPlaceProvider,fixturePlan} from '@travel-blocks/test-fixtures';import type {TripPlanningInput} from '@travel-blocks/shared';import {buildApp} from '../src/app.js';import type {TripRepository} from '../src/repository.js';
class TrackingAi extends TestAiProvider{intentCalls=0;planningInput?:TripPlanningInput;override async extractIntent(){this.intentCalls++;return {destination:{city:'서울'},preferences:['음식'],avoidances:[],requestedCategories:['food' as const]}}override async planTrip(input:TripPlanningInput){this.planningInput=input;return structuredClone(fixturePlan)}}
describe('generate-trip pipeline',()=>{it('intent extraction과 candidate retrieval을 planning context에 연결한다',async()=>{const ai=new TrackingAi();const app=await buildApp({repository:{} as TripRepository,auth:{authenticate:async()=>({userId:'u'})},ai,places:new TestPlaceProvider()});const result=await app.inject({method:'POST',url:'/api/v1/ai/generate-trip',payload:{prompt:'서울 음식 여행'}});expect(result.statusCode).toBe(200);expect(ai.intentCalls).toBe(1);expect(ai.planningInput?.intent.destination?.city).toBe('서울');expect(ai.planningInput?.candidates.length).toBeGreaterThan(0);await app.close()})});
